{
  "name": "taskmaster",
  "private": true,
  "version": "1.0.0",
  "description": "A minimalist and elegant task management application, now with Capacitor.",
  "dependencies": {
    "@capacitor-community/speech-recognition": "^6.0.0",
    "@capacitor/android": "^6.1.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/camera": "^6.0.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/ios": "^6.1.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0",
    "@capacitor/share": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@google/genai": "^0.15.0",
    "@supabase/supabase-js": "^2.43.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.23.1",
    "react-window": "^1.8.10"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.0",
    "esbuild": "^0.21.5"
  },
  "scripts": {
    "dev": "mkdir -p public && cp index.html public/ && ([ -f capacitor.js ] && cp capacitor.js public/ || true) && esbuild index.tsx --bundle --outfile=public/index.js --loader:.tsx=tsx --define:process.env.NODE_ENV='\"development\"' --define:process.env.API_KEY='\"$API_KEY\"' --servedir=public",
    "assets:generate": "capacitor-assets generate --icon-path icon.svg --icon-background-color '#F3F1F8'",
    "sync:ios": "npx cap sync ios",
    "sync:android": "npx cap sync android",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "build": "mkdir -p public && cp index.html public/ && ([ -f capacitor.js ] && cp capacitor.js public/ || true) && esbuild index.tsx --bundle --outfile=public/index.js --loader:.tsx=tsx --define:process.env.NODE_ENV='\"production\"' --define:process.env.API_KEY='\"$API_KEY\"'"
  }
}
